// Copyright (c) 2025 Vitaly Anasenko
// Distributed under the MIT License, see accompanying file LICENSE.txt

#pragma once

#include <string>

namespace Basic {
    constexpr unsigned c_versionMajor { ${PROJECT_VERSION_MAJOR} };
    constexpr unsigned c_versionMinor { ${PROJECT_VERSION_MINOR} };
    constexpr unsigned c_versionPatch { ${PROJECT_VERSION_PATCH} };

    namespace Wcs {
        constexpr std::wstring_view c_version { L"${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}.${PROJECT_VERSION_PATCH}" };
        constexpr std::wstring_view c_buildTimestamp { L"${KKMHA_BUILD_TIMESTAMP}" };
    }
}
